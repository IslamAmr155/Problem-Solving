#include <iostream>
#include <cmath>
#include <string>
#include <algorithm>
#include <limits>
#include <map>
#include <vector>

#define ll long long 

using namespace std;

int main()
{
	int t, a, b, c, d;
	cin >> t;
	while (t--)
	{
		cin >> a >> b >> c >> d;
		cout << b << " " << c << " " << c << endl;
	}
	return 0;
}